<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wakutabi.mapper.TravelCanceledAndEndMapper">
    <!-- 현재 OPEN,MATCHED 여행ID 찾기 -->
    <select id="findOpenAndMatchedArticleIds" resultType="long">
        SELECT id
        FROM trip_article
        WHERE status IN ('OPEN','MATCHED')
        AND NOW() >= end_date
    </select>
    <!-- 현재 CANCELED 여행 ID 찾기 -->
    <select id="findCanceledArticleIds" resultType="long">
        SELECT id
        FROM trip_article
        WHERE status = 'CANCELED'
    </select>
    <!-- 현재 CLOSED 여행 ID 찾기 -->
    <select id="findEndArticleIds" resultType="long">
        SELECT id
        FROM trip_article
        WHERE status = 'CLOSED'
    </select>
    <!-- 종료시간되면 여행 CLOSED로 상태 변경 -->
    <update id="updateEndTravels" parameterType="list">
        <if test="list != null and list.size() > 0">
            UPDATE trip_article
            SET status = 'CLOSED'
            WHERE status IN ('OPEN','MATCHED')
                AND id IN
                <foreach collection = "list" item="id" open = "(" separator="," close=")">
                #{id}
                </foreach>
        </if>
    </update>
</mapper>